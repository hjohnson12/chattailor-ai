namespace ChatTailorAI.Shared.Models.Chat
{
    public interface IChatModelMessage
    {
        // This interface is so that all chat model messages for their API can
        // be used in the same way (will add properties as needed)
    }
}